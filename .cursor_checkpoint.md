# 开发断点快照（Checkpoint Summary）

**项目**：`seo-data-dashboard` · `run_sync.bat` → GSC + Ahrefs → 明道云  
**断点更新**：2026-06-08（本对话框）  
**总览**：**GSC Top 查询 Ahrefs enrich** 已实现并跑通（usa + 点击>0 + Top500 去重调 API）；**明道云日期筛选 bug** 已修（读表/enrich/GSC Top upsert）。页面表仍有 **URL Inspection 429**、Ahrefs 小站空白等待办。运营文档见 `docs/20260608note_optimize.md`、`docs/20260608GSC_top-queries-update.md`。

---

## 1. 本对话框（2026-06-08）

| 项 | 说明 |
| --- | --- |
| **GSC Top enrich** | `enrich_gsc_top_queries()` + `AhrefsClient.fetch_keywords_overview`；写 **Ahrefs搜索量/KD/CPC** |
| **enrich 默认** | `GSC_TOP_QUERIES_ENRICH_COUNTRIES=usa` · `MODE=clicks_gt_zero` · `LIMIT=500`；7 日内 **词+国** 只调 1 次 overview，写回各日期行 |
| **CLI** | `--skip-gsc-top-queries-enrich` · `--only-gsc-top-queries-enrich`（不需重拉 GSC） |
| **报告** | 首行 `Command : run_sync.bat ...` |
| **读表 bug 已修** | 明道云 `getFilterRows` **日期等值筛选无效** → `load_worksheet_rows_in_date_window`（按站拉全量 + 内存按数据日期过滤） |
| **同一天 upsert** | `sync_gsc_top_queries` / `sync_gsc_top_pages` 已用上述读法 → 有行则 `editRow`，**防再叠重复行**；**不自动删** 历史重复行 |
| **跨日期** | **不去重**（7 天 × 词 × 国 = 多行，设计如此） |
| **enrich 首次 0 写入** | 修读表前误报「无目标」；修后 richconn 等可 enrich |
| **Volume 无 KD/CPC** | Ahrefs overview 对品牌/长尾常 `difficulty/cpc=null`；脚本 **有值才写**，非 bug |
| **`.env.example`** | 已备份全部 worksheet/field controlId；敏感项（APP_KEY/SIGN/Ahrefs/GSC 凭证）留空 |
| **页面表 429** | GSC URL Inspection 限流 ~2000 次/天/属性；失败暂写「未收录」——**待改**：429 不写收录状态 + 节流 + 失败行清单 |

### 1.1 GSC Top enrich — 命令（锚点 2026-05-30 = 窗口 5/24～5/30）

```bat
cd /d D:\seo-data-dashboard

:: 五站只 enrich（不重拉 GSC）
run_sync.bat --only-gsc-top-queries-enrich --anchor-date 2026-05-30

:: 单站
run_sync.bat --only-gsc-top-queries-enrich --site richconn --anchor-date 2026-05-30

:: GSC Top 查询 + enrich 一起
run_sync.bat --skip-keywords --skip-pages --skip-backlinks --skip-dashboard --skip-gsc-top-pages --anchor-date 2026-05-30
```

**明道云列（已配置 `.env`）：** `MINGDAO_FIELD_GSC_QUERY_AHREFS_VOLUME/KD/CPC`

### 1.2 页面表 — 分站补跑（仍有效，锚点 2026-05-30）

```bat
run_sync.bat --skip-keywords --skip-backlinks --skip-dashboard --skip-gsc-top-queries --skip-gsc-top-pages --anchor-date 2026-05-30 --site richconn
```

VPN 慢 / 429 后：`MINGDAO_REQUEST_TIMEOUT=180`；Inspection 配额恢复后再跑。

---

## 2. 架构速览

| 层 | 路径 |
| --- | --- |
| 入口 | `run_sync.bat` → `script/sync.py` |
| GSC Top enrich | `enrich_gsc_top_queries` · `fetch_keywords_overview` · `load_worksheet_rows_in_date_window` |
| 页面表 | `sync_pages` → GSC import + 逐行 Inspection + Ahrefs 按 URL |
| 配置 | `.env` + `config/mingdao_worksheets.json` + `config/sites.json` |
| 文档 | `docs/20260608GSC_top-queries-update.md` · `docs/20260608note_optimize.md` · `docs/20260608note_analysis.md` |
| 报告 | `reports/sync-report-*.txt`（首行 Command） |

### GSC Top 查询流水线

```
sync_gsc_top_queries（按日 GSC upsert，键=数据日期+站+词+国）
  → enrich_gsc_top_queries（usa+点击>0，词+国去重调 overview → editRow 三列）
```

---

## 3. 当前数据状态

| 工作表 | 状态 | 备注 |
| --- | --- | --- |
| **GSC Top 查询** | enrich 已跑 | richconn 等 usa 有点击词已有 Ahrefs 三列；部分词仅 Volume（API null KD/CPC） |
| **GSC Top 查询** | 可能有历史重复行 | 同一天同键多行 = 旧 bug 叠行；sync 已修 upsert，**需明道云手动删**多余行 |
| **页面管理表** | GSC 列多有值 | 收录状态受 429 影响可能误标「未收录」 |
| **看板 / 关键词 / 外链** | 见 2026-06-06 状态 | 与 enrich 独立 |

---

## 4. 已知问题与处理

| 现象 | 处理 |
| --- | --- |
| enrich 报告 writes=0 + Skipped「无目标」 | 已修读表；若仍 0：查表内 5/24～5/30 是否有 usa 点击>0 行 |
| 有搜索量无 KD/CPC | Ahrefs API `null`；品牌词 richconn 等正常 |
| GSC Top 同一天重复行 | 手动删重复；再跑 GSC Top sync（会 update 不新增） |
| URL Inspection **429** | 停跑/隔天再跑；勿信 429 期间「未收录」；待实现跳过+补跑 |
| 页面 Ahrefs 列空白（小站） | top-pages=0；见 dashboard §4.3 |
| Mingdao 超时 | `MINGDAO_REQUEST_TIMEOUT=180` |

---

## 5. 接下来可做（按优先级）

### P0

1. 明道云清理 GSC Top 查询 **同一天同键** 重复行（richconn 等）
2. 页面表 429：**429 不写收录状态** + 请求间隔 + 失败 URL 下次优先补

### P1 — 代码

1. 页面表 crawled URL 多语言模糊匹配；metrics 兜底勿用域名级 `org_keywords`
2. enrich 报告汇总计入 `enrich` 写入数（已部分：`enrich` 计 update）
3. 页面字数二期 `site-audit`

### P2

- 看板本站 DR 补跑；GSC Top enrich 扩国家（改 `GSC_TOP_QUERIES_ENRICH_COUNTRIES`）

---

## 6. 关键文件

| 文件 | 用途 |
| --- | --- |
| `script/sync.py` | `enrich_gsc_top_queries`、`load_worksheet_rows_in_date_window`、`sync_gsc_top_queries` |
| `.env` / `.env.example` | Ahrefs enrich 三列 ID + enrich 策略 env |
| `docs/20260608GSC_top-queries-update.md` | enrich 字段、CLI、策略 |
| `docs/20260608note_optimize.md` | 各表高价值用法与查询指南 |

---

## 7. 上对话框摘要（2026-06-06，仍有效）

- 页面表：GSC import + 逐行 update；Ahrefs 空白多因 top-pages=0 / URL 不匹配  
- `docs/dashboard.md` §4.3 API 排错表  
- 页面表分站 `--skip-gsc-top-queries --skip-gsc-top-pages` 见 §1.2  

---

*下次会话：清 GSC Top 重复行；页面 Inspection 429 改进；或 `--only-gsc-top-queries-enrich` 扩国家/重跑。*
