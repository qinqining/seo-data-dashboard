# 开发断点快照（Checkpoint Summary）

**项目**：`seo-data-dashboard` · `run_sync.bat` → GSC + Ahrefs → 明道云  
**断点更新**：2026-06-09（本对话框）  
**总览**：**外链监控表同步已下线**；看板 **GSC 四档周环比列已删除**（不再拉对比日 query）；页面表改为 **Ahrefs `top-pages` import** + 逐行 update；**`.env.example` 已与 `.env` 同步**（敏感项留空）。页面表仍慢（URL Inspection + 明道云）；小站 **top-pages=0** 时仅 seed 首页。

---

## 1. 本对话框（2026-06-09）

### 1.1 代码 / 配置变更

| 项 | 说明 |
| --- | --- |
| **外链监控表** | `sync_backlinks`、CLI `--skip-backlinks`、`config/mingdao_worksheets.json` 的 `backlinks` 段已删；`docs/dashboard.md` 改为七工作表 |
| **看板四档周环比** | 已删 `GSC周环比 Top1-3～Top21-100` 写入；`DashboardFieldIds` 无 `*_wow`；**不再拉 anchor−7 对比日 GSC query**（每站 sync 少 7 次 query API） |
| **看板保留** | **GSC 四档搜索词数**（每日）；锚点日 **周环比流量**、Backlinks变化、收录汇总等 |
| **页面表 import** | 仅 **Ahrefs `top-pages`** 批量 `addRow`（独立站+URL+数据日期）；**不用 GSC page 维发现 URL** |
| **页面表 update** | 逐行：GSC 四指标 + **URL Inspection**（收录）+ Ahrefs 字段 + 全球排名DR |
| **Ahrefs 默认** | `AHREFS_TARGET_COUNTRY=all`（关键词表 + 页面表全球聚合） |
| **关键词「独立站」列 ID** | `MINGDAO_FIELD_KEYWORD_SITE=6a27eb090e52596f861cd7fd`（`.env` + `mingdao_worksheets.json` + `sync.py` env 覆盖） |
| **GSC Top 查询「独立站」** | `MINGDAO_FIELD_GSC_QUERY_SITE=6a27e0130c56f1c763468dfc` |
| **GSC Top 页面「独立站」** | `MINGDAO_FIELD_GSC_PAGE_SITE=6a27e12e611b4ca5b1841560` |
| **页面表「独立站」** | `MINGDAO_FIELD_PAGE_SITE=6a27e7d5b77a8e00c743eacc` |
| **`.env.example`** | 2026-06-09 已与 `.env` 对齐；`MINGDAO_APP_KEY` / `MINGDAO_SIGN` / `AHREFS_API_TOKEN` 留空 |

### 1.2 页面表运维结论

| 现象 | 原因 |
| --- | --- |
| **先 add 三列再 update** | import 占 Upsert 键；update 需 rowId + 每 URL 调 Inspection（设计如此，慢点主要在 Inspection） |
| **richconn ~501 行极慢** | 每 URL Inspection ~6–8s + 明道云 editRow 偶发 8–10min；可 **Ctrl+C 中断**，已 update 行保留 |
| **lasermicrofab 仅首页** | 2026-06-09 报告：`top-pages urls=0`、`ahrefs_import=0`；GSC 有 17 page 但 **不用于 import**；下午曾 `--clear-pages` 删过旧行 |
| **fecision 无 Ahrefs页面类型** | API `page_type` 全为 `null`（小站正常），非脚本 bug |
| **锚点日** | 页面表 **单日快照**（非 7 日行）；要 5/24–5/30 窗口用 `--anchor-date 2026-05-30` |

**待选优化（未做）**：`--skip-page-index`；Ahrefs `top-pages=0` 时用 **GSC page** 或 **crawled-pages** 兜底 import；续跑跳过已更新行。

### 1.3 关键词库

- **upsert**：有则更新、无则新建；Ahrefs 已无的词 **不自动删**
- **五站分站**（锚点 2026-05-30）：

```bat
run_sync.bat --skip-pages --skip-dashboard --skip-gsc-top-queries --skip-gsc-top-pages --skip-gsc-top-queries-enrich --anchor-date 2026-05-30 --site cncpioneer
run_sync.bat --skip-pages --skip-dashboard --skip-gsc-top-queries --skip-gsc-top-pages --skip-gsc-top-queries-enrich --anchor-date 2026-05-30 --site fecision
run_sync.bat --skip-pages --skip-dashboard --skip-gsc-top-queries --skip-gsc-top-pages --skip-gsc-top-queries-enrich --anchor-date 2026-05-30 --site richconn
run_sync.bat --skip-pages --skip-dashboard --skip-gsc-top-queries --skip-gsc-top-pages --skip-gsc-top-queries-enrich --anchor-date 2026-05-30 --site lasermicrofab
run_sync.bat --skip-pages --skip-dashboard --skip-gsc-top-queries --skip-gsc-top-pages --skip-gsc-top-queries-enrich --anchor-date 2026-05-30 --site drametal
```

- `.env` 的 `SYNC_SITES` 默认 **不含 drametal**，全跑四站可不加 `--site`；drametal 须单独 `--site drametal` 或改 `SYNC_SITES`

### 1.4 页面表 — 常用命令

```bat
:: 单站页面表（勿再用 --skip-backlinks，已不存在）
run_sync.bat --skip-keywords --skip-dashboard --skip-gsc-top-queries --skip-gsc-top-pages --anchor-date 2026-05-30 --site lasermicrofab

:: 清空页面表（危险）
run_sync.bat --clear-pages --clear-pages-dry-run
run_sync.bat --clear-pages --yes --site fecision
```

### 1.5 看板 — 仅 GSC 四档词数（无周环比）

```bat
run_sync.bat --only-dashboard-gsc-buckets --anchor-date 2026-05-30
```

### 1.6 CLI 注意

- **`--skip-backlinks` 已移除**（会报 `unrecognized arguments`）
- 站点 key 拼写：`lasermicrofab`（不是 lasermicofab）

### 1.7 字段 ID 速查（`.env`）

| 表 | 独立站 env 键 |
| --- | --- |
| SEO 自动数据看板 | `MINGDAO_FIELD_DASH_SITE` |
| 站点关键词库 | `MINGDAO_FIELD_KEYWORD_SITE` |
| GSC Top 查询 | `MINGDAO_FIELD_GSC_QUERY_SITE` |
| GSC Top 页面 | `MINGDAO_FIELD_GSC_PAGE_SITE` |
| 页面管理表 | `MINGDAO_FIELD_PAGE_SITE` |

选项 UUID（五站单选 **值**）仍在 `config/mingdao_worksheets.json` → 各表 `site_option_keys`。

---

## 2. 架构速览（当前）

| 模块 | 脚本 | 说明 |
| --- | --- | --- |
| 看板 | `sync_dashboard` | GSC 7 日 + 四档词数 + 锚点日 RD/流量环比/收录 |
| 关键词库 | `sync_keywords` | Ahrefs organic-keywords（`all`） |
| 页面表 | `sync_pages` | top-pages import → 逐行 update |
| GSC Top 查询 | `sync_gsc_top_queries` + `enrich_gsc_top_queries` | 7 日窗口 |
| GSC Top 页面 | `sync_gsc_top_pages` | 7 日窗口 |
| ~~外链监控表~~ | **已删除** | — |

**入口**：`run_sync.bat` → `script/sync.py`  
**配置**：`.env` + `config/mingdao_worksheets.json` + `config/sites.json` + `config/mingdao_options.json`  
**文档**：`docs/dashboard.md`（2026-06-09 版）  
**报告**：`reports/sync-report-*.txt`（首行 `Command :`）

---

## 3. 当前数据 / 运维状态

| 工作表 | 状态 | 备注 |
| --- | --- | --- |
| **页面 richconn** | 2026-06-09 下午 import 500 行，update ~55% 后可能中断 | 可续跑同命令 |
| **页面 lasermicrofab** | 仅首页 1 行 | Ahrefs top-pages=0 |
| **页面 cncpioneer** | 2026-06-09 17:39 报告 5 行正常 | top-pages=4 |
| **关键词库** | 用户计划重跑五站 | 见 §1.3 |
| **GSC Top 查询 enrich** | 2026-06-08 已跑通 | 见旧 checkpoint §1.1 |
| **看板** | 四档周环比列明道云可删列；脚本不再写 | |

**环境**：`SYNC_SITES=cncpioneer,fecision,richconn,lasermicrofab`（跳过 drametal GSC 403）  
**VPN / 超时**：`MINGDAO_REQUEST_TIMEOUT=180`；GSC 走系统代理 `GOOGLE_PROXY=` 空

---

## 4. 已知问题与处理

| 现象 | 处理 |
| --- | --- |
| URL Inspection **429** | 停跑/隔天；429 期间「未收录」不可信；待 P0：429 不写收录 + 节流 |
| 页面 sync 极慢 | 正常；五站×500 URL 可数十小时；分站跑、降 `PAGES_AHREFS_IMPORT_LIMIT` |
| top-pages=0 仅首页 | 小站；人工加 URL 或待 GSC/crawled 兜底 import |
| Ahrefs页面类型空白 | API `page_type=null`；用运营页面类型 |
| Mingdao editRow 卡数分钟 | 网络/API；续跑安全 |
| GSC Top 历史重复行 | 手动删；sync 已修 upsert（2026-06-08） |
| drametal GSC | 暂排除在 `SYNC_SITES` 外 |

---

## 5. 接下来可做（按优先级）

### P0

1. 页面表 429：失败不写收录 + 请求间隔 + 失败 URL 清单/补跑
2. richconn / 其它站页面 update **续跑**至 `updated≈行数`

### P1

1. **top-pages=0 兜底**：GSC page 或 crawled-pages import（lasermicrofab 类小站）
2. **`--skip-page-index`** 或 Inspection 7 日缓存（大幅提速）
3. 续跑时 **跳过 data_date=锚点日** 已更新行
4. 明道云清理 GSC Top 查询同一天重复行

### P2

- lasermicrofab 页面表恢复多 URL（人工或兜底 import）
- GSC Top enrich 扩国家
- 页面字数 `site-audit`

---

## 6. 关键文件

| 文件 | 用途 |
| --- | --- |
| `script/sync.py` | 主同步；已无 backlinks / 四档 wow |
| `.env` / `.env.example` | 字段 ID + Ahrefs/GSC 策略（example 无密钥） |
| `config/mingdao_worksheets.json` | 表 worksheet + site/country option_keys |
| `docs/dashboard.md` | 字段口径、命令、排错 |
| `docs/20260608GSC_top-queries-update.md` | GSC Top enrich |
| `docs/20260608note_optimize.md` | 运营查询指南 |

---

## 7. 上对话框摘要（仍有效）

- **2026-06-08**：GSC Top enrich；明道云日期筛选 bug 修；`load_worksheet_rows_in_date_window`；同一天 upsert 防叠行
- **Cursor**：长对话会 **自动压缩上下文**；重要结论写进本文档 / `docs/`

---

*下次会话：关键词五站跑完核对 report；richconn 页面续跑；或实现 top-pages=0 / Inspection 提速。*
